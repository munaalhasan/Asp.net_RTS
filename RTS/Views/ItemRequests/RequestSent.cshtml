@model IEnumerable<RTS.DataAccess.Models.Transaction>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutWithLeftMenu";
}

<h1>Requests Sent</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Item.Name)*@
                To
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Item.Name)*@
                Item
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Status.StatusType)*@
                Status
            </th>            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.EmployeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemRequest.Item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status.StatusType)
            </td>           
            @*<td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>*@
        </tr>
}
    </tbody>
</table>
